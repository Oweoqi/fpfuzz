# fpfuzz
```
usage: fpfuzz.py [-h] -i PAYLOAD -u URL [-v]

Simple as heck URL fuzzer

optional arguments:
  -h, --help  show this help message and exit
  -i          PAYLOAD
  -u          URL
  -v          show program's version number and exit
```
